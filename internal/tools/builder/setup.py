# This is a placeholder file
# The actual build process is handled by the GitHub Actions workflow

from setuptools import setup

if __name__ == "__main__":
    setup(
        name="poma-chunker-builder",
        version="0.1.0",
        description="Build tools for poma-chunker",
    )
